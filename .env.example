# Server Configuration
# Port the server will listen on (usually set by hosting platform)
PORT=8080
# Host address to bind to (0.0.0.0 binds to all interfaces)
HOST=0.0.0.0
# Log level for application logs
# Options: debug, info, warn, error, dpanic, panic, fatal
LOG_LEVEL=info

# Firebase Project ID (used for Cloud Trace correlation in structured logging)
# In development (APP_ENVIRONMENT=development), this can be omitted to use "demo-test-project"
# Note: Project IDs with "demo-" prefix trigger Firebase emulator-only mode - the SDK will
# only communicate with local emulators and fail if an emulator isn't running for a service.
# In other environments, this is required or the server will fail to start
FIREBASE_PROJECT_ID=demo-test-project
# Firebase Project Number
FIREBASE_PROJECT_NUMBER=demo-firebase-project-number

# Enable Secret Manager integration (true/false)
SECRET_MANAGER_ENABLED=false

# Application environment label (development, staging, production)
APP_ENVIRONMENT=development
# Base URL for the application
APP_URL=http://localhost:8080
