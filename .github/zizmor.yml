rules:
  dependabot-cooldown:
    config:
      days: 1
  unpinned-uses:
    config:
      policies:
        # Repository policy requires explicit full release tags maintained by Dependabot.
        "*": ref-pin
